name: obesity_model

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators: {type: int}
      max_depth: {type: int}
      dataset: {type: str}
    command: >
      python modelling.py {n_estimators} {max_depth} {dataset}
